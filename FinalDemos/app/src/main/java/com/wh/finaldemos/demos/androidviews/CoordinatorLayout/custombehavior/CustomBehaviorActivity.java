package com.wh.finaldemos.demos.androidviews.CoordinatorLayout.custombehavior;

import android.os.Bundle;

import com.wh.finaldemos.BaseDemoActivity;
import com.wh.finaldemos.R;

public class CustomBehaviorActivity extends BaseDemoActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_custom_behavior);
    }
}
